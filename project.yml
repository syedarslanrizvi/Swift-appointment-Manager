name: MeetMate
options:
  bundleIdPrefix: com.example
targets:
  MeetMate:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - ./
    settings:
      base:
        SWIFT_VERSION: 5.0 